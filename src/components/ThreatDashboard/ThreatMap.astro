---
---
<div class="w-full h-[400px] bg-gray-100 rounded-lg relative" id="threatMap">
  <div class="absolute inset-0 flex items-center justify-center text-gray-500">
    Loading threat map...
  </div>
</div>

<script>
import { Chart } from 'chart.js/auto';

async function initMap() {
  try {
    const response = await fetch('/api/threats');
    const threats = await response.json();
    
    // Process threat data for map visualization
    // Implementation will be added when geographic data is available
    
  } catch (error) {
    console.error('Failed to load threat map:', error);
  }
}

initMap();
</script>